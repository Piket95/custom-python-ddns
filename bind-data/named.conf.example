options {
    recursion no;
    allow-query { none; };
};
zone "dns.<domain>" IN { //dns can be any subdomain of your liking
    type master;
    file "/etc/bind/<domain>.zone"; //replace with your created zone file (example file can be your blueprint)
    allow-query { any; };
    allow-update { localhost; };
};